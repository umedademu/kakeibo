# データ保管

## 保管先

CloudflareのD1データベースを使用します。

- データベース名: `kakeibo-db`
- 作成日: 2026年9月3日
- 保存場所: Cloudflareによる自動配置

## 保存する内容

- 各項目の現在残高
- 残高を変更した日時と変更後の金額
- 毎日午前4時に確定する日別残高

日別残高は「日付と項目」の組み合わせを1件だけ保存し、定期処理が再実行されても重複しないようにします。

## 接続方法

Vercel上のアプリからCloudflare Workerを経由してD1へ読み書きします。VercelとWorkerの間は、両方に登録した秘密の文字列で保護します。

ブラウザにはD1の接続情報や秘密の文字列を渡しません。

## 日別残高の確定

Cloudflareの定期処理を毎日19時（世界標準時）に実行します。これは日本時間の翌日午前4時に相当します。

その時点の最新残高を、日本時間で見た前日の日別残高として保存します。財布の残高に変更がない場合も、同じ金額を保存します。

貯玉については、定期処理の開始時に収支管理アプリのCloudflare Workerから「全店舗合計の貯玉」を円単位で取得します。取得した最新金額で現在残高を更新してから、前日分の日別残高を保存します。

収支管理アプリとの通信には、Cloudflare内の専用接続と、両方のCloudflare Workerだけが保持する秘密情報を使用します。ブラウザにはこの情報を渡しません。

## 端末間の共有

パソコンで財布残高を変更するとD1へ即時保存されます。スマートフォンでアプリを開いたときも同じD1から読み込むため、同じ金額が表示されます。

## 今後の予定

- 直近30日など、期間を指定して残高の推移を取得できるようにする
- PayPay、PayPay銀行、FX口座の入力方法を追加する
